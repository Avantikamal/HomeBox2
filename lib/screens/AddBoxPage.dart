import 'package:flutter/material.dart';

class AddBox extends StatelessWidget{
  @override
  Widget build(BuildContext context)
  {
    return Scaffold(
      body: Container(
        child:Center(child: Text("AddBox PAge"),)
      ),
    );
  }
}